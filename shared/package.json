{
  "name": "@zslack/shared",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "react-native": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./auth": "./src/auth.ts",
    "./db": "./src/db/index.ts"
  },
  "scripts": {
    "generate": "bun run generate:zero && bun run generate:db",
    "generate:zero": "drizzle-zero generate -s ./src/db/schema/public.ts -o ./src/zero-schema.gen.ts -f",
    "generate:db": "drizzle-kit generate"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "drizzle-zero": "^0.17.0-canary.2",
    "pg": "^8.16.3",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.8"
  }
}
